# cloudbuild.yaml
substitutions:
  _IMAGE_PATH: ""

steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - "-t"
      - "${_IMAGE_PATH}"
      - "."

images:
  - "${_IMAGE_PATH}"
